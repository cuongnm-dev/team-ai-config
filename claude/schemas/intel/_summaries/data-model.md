# Schema Summary — Data Model

> **AUTO-GENERATED** by `extract_schema_summary.py` at 2026-04-28T16:26:55Z.
> **DO NOT EDIT** — edit canonical schema instead, then re-run script.
> **Source**: `~/.claude/schemas/intel/data-model.schema.json` (schema_version: `1.0`)
> **Consumed by** (Diátaxis tag — D1): TKCT (§4 entirely), TKKT (§5 aggregated)
> **Writer voice hint**: Reference (DDL detail for engineers)

## Purpose

Logical + physical data model: entities, tables, columns, ERD, data dictionary, backup strategy. Tier 2 cross-stage. Consumed by sa-pro/data-governance/dev (Cursor when migration) + tdoc-tkct-writer §4 (entirely) + tdoc-tkkt-writer §5 (aggregated). Aggregated from code-facts.entities[] by from-code, augmented with business meaning by from-doc. Justified by TKCT §4.1 (mô hình DL logic), §4.2 (mô hình DL vật lý), §4.3 (từ điển dữ liệu), §4.4 (backup/recovery); TKKT §5 (aggregated entity catalog).

## Required top-level fields

- `schema_version`
- `entities`
- `tables`

## Field structure (depth ≤ 2)

- `schema_version` (string) [const='1.0'] **REQUIRED**
- `erd_diagram` (string | null) — Mermaid erDiagram source for top 10-15 entities + relationships. Source for TKCT §4.1 + TKKT §5.1 sơ đồ ER.
- `entities` (array<object {...}>) **REQUIRED** — Logical entity catalog (aggregated, business-level). Used by TKKT §5 (no column-level — TKCT territory) + feature-catalog.features[].entities[] FK target.
  - **(each array item)**:
    - `name` (string) **REQUIRED** — PascalCase entity name (e.g. 'User', 'ApprovalRequest'). Stable across regenerations.
    - `name_vn` (string | null) — Vietnamese display name for TKCT §4.3 + TKKT §5.
    - `purpose` (string) [min_chars=30] **REQUIRED** — Business meaning of this entity. Source for TKCT §4.3 'business_meaning' column. Min 30 chars to prevent thin output.
    - `category` (string) — TKKT §5.2 phân loại.
    - `owning_service` (string | null) — system-inventory.services[].id that owns this entity (writer responsibility).
    - `owning_component` (string | null) — architecture.components[].name that owns this entity. Cross-ref TKKT §4.2.
    - `key_relations` (array<string>) — Top 3-5 relationships (e.g. 'User has-many ApprovalRequest'). Used in TKKT §5.1 narrative.
    - `table_refs` (array<string>) — tables[].name that physically implement this entity. Usually 1:1 but may be 1:N for sharded/partitioned.
    - `evidence` (array<$ref:evidence>)
    - `confidence` (string)
    - `source_producers` (array<string>)
- `tables` (array<object {...}>) **REQUIRED** — Physical table-level DDL detail. TKCT §4.2 mandate: per-table columns + types + constraints + indexes. Hard-stop in from-code Phase 8 if empty when project has DB.
  - **(each array item)**:
    - `name` (string) **REQUIRED** — Physical table name (db-native casing).
    - `schema` (string | null) — DB schema/namespace (e.g. 'public', 'dbo').
    - `entity_ref` (string | null) — entities[].name this table implements.
    - `description` (string) — Table purpose (1-2 sentences).
    - `columns` (array<object {...}>) [min_items=1] **REQUIRED**
    - `indexes` (array<object {...}>)
    - `constraints` (array<object {...}>)
    - `row_count_estimate` (integer | null) [min=0] — Used for TKCS §4.2 sizing + TKCT §6.2 capacity planning.
    - `growth_estimate` (string | null) — Free-form: 'low', '~10K rows/month', etc.
    - `partitioning` (object | null)
    - `evidence` (array<$ref:evidence>)
    - `confidence` (string)
- `data_dictionary` (array<object {...}>) — Semantic data dictionary — TKCT §4.3 mandate. Augmented business meaning per column. Generated by merging tables[].columns with business prose from from-doc.
  - **(each array item)**:
    - `table` (string) **REQUIRED**
    - `column` (string) **REQUIRED**
    - `type` (string) **REQUIRED**
    - `business_meaning` (string) [min_chars=20] **REQUIRED** — Vietnamese; what does this column mean to the business?
    - `valid_values` (array<string | number>)
    - `format_pattern` (string | null) — Regex or format hint (e.g. 'YYYY-MM-DD', 'CCCD 12 digits').
    - `is_required` (boolean)
    - `example_values` (array<string | number | boolean>)
- `data_classification` (object {...}) — Data sensitivity classification per NĐ 13/2023 (DLCN) + NĐ 85/2016 (ATTT). TKCT §5.3 mandate.
  - `scheme` (string) — Classification scheme used.
  - `levels` (array<object {...}>)
- `backup_strategy` (object {...}) — Backup + recovery design. TKCT §4.4 mandate.
  - `frequency` (string | null) — Free-form: 'daily', 'every 6h', 'continuous'.
  - `method` (enum (one of 8 values))
  - `retention_policy` (string | null) — Retention rule. Example: '30 days hot + 1 year cold'.
  - `storage_location` (string | null) — Where backups are stored.
  - `rpo_minutes` (integer | null) [min=0] — Recovery Point Objective in minutes.
  - `rto_minutes` (integer | null) [min=0] — Recovery Time Objective in minutes.
  - `encryption` (boolean)
  - `tested_at` (string | null) [format=date] — Last DR drill date.
- `migrations` (array<object {...}>) — Migration history (chronological). Optional. Source for TKCT §4 schema evolution narrative + drift tracking.
  - **(each array item)**:
    - `version` (string) **REQUIRED** — Migration version/timestamp id.
    - `file` (string) **REQUIRED** — Repo-relative path.
    - `applied_at` (string | null) [format=date-time]
    - `description` (string)
    - `tables_affected` (array<string>)
- `warnings` (array<object {...}>)
  - **(each array item)**:
    - `severity` (enum ['high', 'medium', 'low']) **REQUIRED**
    - `category` (string)
    - `message` (string) **REQUIRED**
    - `table` (string | null)
    - `column` (string | null)

## Critical constraints (quick reference for emit/validate)

- `entities[].purpose` — min_chars=30
- `tables[].columns` — min_items=1
- `tables[].indexes[].columns` — min_items=1
- `data_dictionary[].business_meaning` — min_chars=20

---

**Sub-agent prompt usage** (D6 — compile-time inline injection):

Embed this summary verbatim trong sub-agent prompt khi sub-agent emit `data-model.json`.
Output JSON MUST match constraints listed above. Run validate.py post-hoc.

For full schema: `~/.claude/schemas/intel/data-model.schema.json`